import path from 'node:path';
// import fs from 'fs/promises';
export const PATH_DB = path.join('src', 'db', 'db.json');
// export const prevContacts = await fs.readFile(PATH_DB);
// export const contacts = JSON.parse(prevContacts);